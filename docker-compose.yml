version: '3.4'

services:
  dockerapitest:
    image: hakart/dockerapitest:test-${BUILD_NUMBER}
    build:
      context: .
      dockerfile: DockerAPITest\Dockerfile
    ports:
      - "8080:80"
    
